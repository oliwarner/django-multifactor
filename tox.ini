[tox]
isolated_build = true
requires = coverage
skip_missing_interpreters = true
envlist =
    py310-django52
    py311-django52
    py312-django{60,52}
    py313-django(60,52)
    py314-django{60,52}
    py315-django{60,52}

[pytest]
django_find_project = false

[testenv]
deps =
    pytest
    pytest-cov
    pytest-django
    django-decorator-include
    django_extensions
    django-debug-toolbar
    django52: Django>=5.2,<5.3
    django60: Django>=6.0,<6.1

setenv =
    DJANGO_SETTINGS_MODULE = testsite.testsite.settings
    PYTHONPATH = {toxinidir}
commands =
    python -m coverage run -m pytest {posargs}

[testenv:py310-django60]
skip_install = true
commands = python -c "raise SystemExit('Django 6.0 is not supported on Python 3.10')"

[testenv:py311-django60]
skip_install = true
commands = python -c "raise SystemExit('Django 6.0 is not supported on Python 3.11')"